import React from 'react'
import './Card.css'

const Card = ({ id, name, surName, number, type, online, active }) => {

  console.log(id, name, surName, number, type, online, active)
  return (
    <>
      <div className='admin-card'>
        
      </div>
    </>
  )
}

export default Card